#include "monty.h"

/**
 * my_nop - for the Monty opcode 'nop'
 * it performs no operations
 * @stack: pointer to the top node of the list.
 * @line_number: current line number
 */
void my_nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}

